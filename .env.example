# Database URL for Prisma / SQLite
# Use an absolute path for reliable operation with Next.js dev server:
# DATABASE_URL="file:/absolute/path/to/project/prisma/dev.db"
#
# Or use a relative path (works with next build/start, may need absolute for dev):
DATABASE_URL="file:./prisma/dev.db"
